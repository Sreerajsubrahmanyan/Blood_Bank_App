<div class="container mt-4">
  <div class="card shadow-sm p-4">
    <h2 class="text-danger text-center">Donate Blood</h2>

    <%= form_with model: @donation, local: true, class: "mt-3" do |f| %>
      
      <div class="mb-3">
        <%= f.label :name, class: "form-label" %>
        <%= f.text_field :name, class: "form-control", required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :age, class: "form-label" %>
        <%= f.number_field :age, class: "form-control", min: 18, max: 65, required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :weight, class: "form-label" %>
        <%= f.number_field :weight, class: "form-control", min: 50, required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :blood_group, class: "form-label" %>
        <%= f.select :blood_group, Donation::BLOOD_GROUPS, { prompt: "Select Blood Group" }, class: "form-select", required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :last_donation_date, class: "form-label" %>
        <%= f.date_field :last_donation_date, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :location, class: "form-label" %>
        <%= f.text_field :location, class: "form-control", required: true %>
      </div>

      <div class="d-grid">
        <%= f.submit "Donate", class: "btn btn-danger" %>
      </div>

    <% end %>

    <div class="text-center mt-3">
      <%= link_to "Back to Home", root_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
